{
  "name": "backend-boilerplate-django",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && pip3 install -r requirements.txt && python3 manage.py migrate",
    "start": "cd app && python3 manage.py runserver",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^9.7.0"
  }
}
